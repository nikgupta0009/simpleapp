language: node_js

node_js:
  - 5.3

build:
  ci:
    - sudo apt-get install ruby-full
    - npm install nsp
    - gem install sass
    - npm install
    - npm test
    - echo 'CI running'
  on_success:
    - git push https://git.heroku.com/stormy-shelf-52929.git master
